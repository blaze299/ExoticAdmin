<Window x:Class="ExoticAdmin.OrderEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Bérlés Szerkesztése" Height="500" Width="400"
        Background="{StaticResource PrimaryBlack}" WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <Style x:Key="FlatButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.8"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="0,15,0,5"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#222"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
            <Setter Property="BorderBrush" Value="#444"/>
            <Setter Property="FontSize" Value="14"/>
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource AccentGold}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="BÉRLÉS KEZELÉSE" Foreground="{StaticResource AccentGold}" FontSize="22" FontWeight="Bold" Margin="0,0,0,20"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="0,0,15,0">
            <StackPanel>
                <TextBlock Text="Ügyfél neve"/>
                <ComboBox x:Name="cmbCustomer" DisplayMemberPath="Name" SelectedValuePath="Id" Height="38" Padding="5" FontSize="14" Margin="0,0,0,5"/>

                <TextBlock Text="Kibérelt Autó"/>
                <ComboBox x:Name="cmbVehicle" DisplayMemberPath="Name" SelectedValuePath="Id" Height="38" Padding="5" FontSize="14" Margin="0,0,0,5"/>

                <TextBlock Text="Kezdés dátuma (ÉÉÉÉ-HH-NN)"/>
                <TextBox x:Name="txtStart"/>

                <TextBlock Text="Befejezés dátuma (ÉÉÉÉ-HH-NN)"/>
                <TextBox x:Name="txtEnd"/>

                <TextBlock Text="Végösszeg (€)"/>
                <TextBox x:Name="txtPrice"/>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Mégse" Style="{StaticResource FlatButtonStyle}" Width="80" Padding="12" Background="#444" Foreground="White" BorderThickness="0" Margin="0,0,10,0" Click="BtnCancel_Click" Cursor="Hand"/>
            <Button Content="Mentés" Style="{StaticResource FlatButtonStyle}" Width="100" Padding="12" Background="{StaticResource AccentGold}" Foreground="Black" FontWeight="Bold" BorderThickness="0" Click="BtnSave_Click" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>